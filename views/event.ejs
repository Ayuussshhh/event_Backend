<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Event Booking</title>
</head>
<body>
    <h1>Event Booking</h1>
    
    <h3><%= event.title %></h3> <!-- Event Title (Hackathon) -->
    <p>Price: <%= event.price %>/-</p> <!-- Event Price -->
    <p>Quantity: <%= event.quantity %></p> <!-- Available Tickets Quantity -->
    
    <!-- Booking Form -->
    <form action="/checkout" method="post">
        <!-- Hidden fields for event details to process checkout -->
        <input type="hidden" name="eventId" value="<%= event._id %>">
        <input type="hidden" name="price" value="<%= event.price %>">
        <input type="hidden" name="quantity" value="<%= event.quantity %>">

        <input type="submit" value="Proceed to Checkout">
    </form>
</body>
</html>
